import React from "react";

import ReactRgue from "./pages/ReactRogue";

const App = () => (
  <div className="App">
    <header className="App-header">
      <ReactRgue width={40} height={40} tilesize={16} />
    </header>
  </div>
);

export default App;
